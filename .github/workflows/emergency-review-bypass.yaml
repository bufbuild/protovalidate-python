name: Emergency review bypass
on:
  pull_request:
    types:
      - labeled
permissions:
  pull-requests: write
jobs:
  approve:
    name: Approve
    if: github.event.label.name == 'Emergency Bypass Review'
    uses: bufbuild/base-workflows/.github/workflows/emergency-review-bypass.yaml@7c0b54b718244c78f00037343ff7cb33ef7caca9 # main
    secrets: inherit
